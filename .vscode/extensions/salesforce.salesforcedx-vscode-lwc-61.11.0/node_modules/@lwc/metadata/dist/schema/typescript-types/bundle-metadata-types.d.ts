import { CompilerDiagnostic } from '@lwc/errors';
import { ScriptFile } from './script-metadata-types';
import { HTMLTemplateFile } from './template-metadata-types';
import { CSSFile } from './style-metadata-types';
interface BundleMetadata {
    version: string;
    moduleSpecifier: string;
    name: string;
    namespace: string;
    entryFileName?: string;
    files: (HTMLTemplateFile | ScriptFile | CSSFile)[];
    diagnostics: CompilerDiagnostic[];
}
export { BundleMetadata };
