import { SourceLocation } from './common-metadata-types';
export interface GQLTag {
    hash: string;
    hasExpressions: boolean;
    location: SourceLocation;
}
export interface LuvioMetadata {
    gqlTag: GQLTag[];
}
